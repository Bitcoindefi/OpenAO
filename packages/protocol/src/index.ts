/**
 * @openao/protocol
 *
 * Shared packet IDs (opcodes) for OpenAO client and server.
 * Single source of truth for the binary protocol opcodes.
 */
export { CLIENT_PACKET_ID, SERVER_PACKET_ID } from "./packetIds";
export type { ClientPacketID, ServerPacketID } from "./packetIds";
